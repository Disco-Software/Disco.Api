import 'package:flutter/material.dart';

class UnicornImage extends StatelessWidget {
  const UnicornImage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
